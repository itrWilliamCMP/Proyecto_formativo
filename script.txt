CREATE TABLE USUARIOS2ProyectoFormativo(
UUID_Usuario VARCHAR2(50) PRIMARY KEY,
correoElectronico VARCHAR2(100) NOT NULL UNIQUE,
contrasena VARCHAR2(255) NOT NULL
);

CREATE TABLE PACIENTES(
UUID VARCHAR2(60),
NOMBRES VARCHAR2(100),
APELLIDOS VARCHAR2(100),
EDAD INT,
ENFERMEDAD VARCHAR2(150),
NUMEROHABITACION INT,
NUMEROCAMA INT,
MEDICAMENTOASIGNADOS VARCHAR2(100),
HORAAPLICACION VARCHAR2(100)
);

DROP TABLE USUARIOS2ProyectoFormativo

select * from PACIENTES

Select * From USUARIOS2ProyectoFormativo
